export interface IShipping {
    id: string | null,
    name: string
}

export interface ICountry {
    active_currency: any
    code: string,
    code3: string
    country: string
    currency_id: string,
    enabled: string,
    id: string
    is_fraudlent: string
}